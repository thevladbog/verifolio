# In-cell backend address; the Next server proxies /api/* here.
BACKEND_INTERNAL_URL=http://localhost:8080
# Regional cell identity (display only; never used to route data cross-cell).
NEXT_PUBLIC_REGION=local
