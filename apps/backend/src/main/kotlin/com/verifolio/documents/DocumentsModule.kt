/**
 * Documents module: documents, document versions, generated letters,
 * version locking, document status, and public verification page core data.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.documents

/** Module marker — declares the documents bounded context to Spring Modulith. */
internal object DocumentsModule
