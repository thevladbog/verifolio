/**
 * Identity module: user accounts, magic-link authentication, sessions,
 * invitation tokens, recommender access tokens, and login/logout flows.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.identity

/** Module marker — declares the identity bounded context to Spring Modulith. */
internal object IdentityModule
