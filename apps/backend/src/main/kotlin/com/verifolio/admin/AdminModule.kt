/**
 * Admin module: internal support tools, moderation,
 * operational dashboards, and controlled support actions.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.admin

/** Module marker — declares the admin bounded context to Spring Modulith. */
internal object AdminModule
