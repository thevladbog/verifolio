/**
 * Files module: file metadata, object storage abstraction,
 * upload/download flows, file hashes, and file access policy.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.files

/** Module marker — declares the files bounded context to Spring Modulith. */
internal object FilesModule
