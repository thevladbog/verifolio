/**
 * Requests module: reference request lifecycle, reference responses, consent records,
 * selected template, requester context, recommender invitation state, and request status transitions.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.requests

/** Module marker — declares the requests bounded context to Spring Modulith. */
internal object RequestsModule
