/**
 * Profiles module: person profiles, profile verification state,
 * public/private profile fields, professional links, and profile trust summary presentation.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.profiles

/** Module marker — declares the profiles bounded context to Spring Modulith. */
internal object ProfilesModule
