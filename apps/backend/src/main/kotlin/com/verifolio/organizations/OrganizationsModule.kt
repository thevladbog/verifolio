/**
 * Organizations module: organization records, domains, company metadata,
 * and organization verification signals.
 * MVP scope: Organization entity and domain records only.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.organizations

/** Module marker — declares the organizations bounded context to Spring Modulith. */
internal object OrganizationsModule
