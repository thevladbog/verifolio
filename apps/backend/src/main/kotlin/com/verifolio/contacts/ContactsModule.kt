/**
 * Contacts module: recommender contacts, relationship metadata,
 * contact invitation state, and communication preferences.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.contacts

/** Module marker — declares the contacts bounded context to Spring Modulith. */
internal object ContactsModule
