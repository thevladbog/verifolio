/**
 * Signatures module: detached signatures, signature file relationships,
 * signature verification results, certificate metadata, and region-specific signature providers.
 * Boundary rules: see docs/MODULES.md and docs/ARCHITECTURE.md.
 */
package com.verifolio.signatures

/** Module marker — declares the signatures bounded context to Spring Modulith. */
internal object SignaturesModule
